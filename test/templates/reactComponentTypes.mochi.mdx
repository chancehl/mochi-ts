export type COMPONENT_NAMEProps = {
    id: string,
}

```mochi
{
    "templateName": "react-component-types",
    "fileName": "COMPONENT_NAME.types.ts",
    "include": ["react-component"],
    "tokens": ["COMPONENT_NAME"]
}
```
