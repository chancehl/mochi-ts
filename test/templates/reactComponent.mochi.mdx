import React from 'react';

import { COMPONENT_NAMEProps } from './COMPONENT_NAME.types.ts'

const COMPONENT_NAME = (props: COMPONENT_NAMEProps) => {
    return (
        <>
            Hello world, I am COMPONENT_NAME
        </>
    )
}

export default COMPONENT_NAME

```mochi
{
    "templateName": "react-component",
    "fileName": "COMPONENT_NAME.tsx",
    "include": ["react-component-types"],
    "tokens": ["COMPONENT_NAME"]
}
```